# Migration `20190725131044-init`

This migration has been generated by Errorname at 7/25/2019, 1:10:44 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:management"."Tenant"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,"provider" TEXT NOT NULL DEFAULT '' ,"url" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:management"."Tenant.id._UNIQUE" ON "Tenant"("id")

CREATE UNIQUE INDEX "file:management"."Tenant.name._UNIQUE" ON "Tenant"("name")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190725131044-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,16 @@
+datasource management {
+  provider = env("PMT-PROVIDER")
+  url      = env("PMT-URL")
+}
+
+generator photon {
+  provider = "photonjs"
+  output   = env("PMT-OUTPUT")
+}
+
+model Tenant {
+  id       String @default(cuid()) @id @unique
+  name     String @unique
+  provider String
+  url      String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190725131044-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190725131044-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
