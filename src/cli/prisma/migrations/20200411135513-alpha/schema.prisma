datasource management {
  provider = "sqlite"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
  output   = env("PMT_OUTPUT")
}

model Tenant {
  id       String @default(cuid()) @id
  name     String @unique
  provider String
  url      String
}