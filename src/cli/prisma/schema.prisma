datasource management {
  provider = "to_be_defined"
  url      = env("PMT_MANAGEMENT_URL")
}

generator client {
  provider      = "prisma-client-js"
  output        = env("PMT_OUTPUT")
  binaryTargets = ["native"]
}

model Tenant {
  id       String @default(cuid()) @id
  name     String @unique
  provider String
  url      String
}