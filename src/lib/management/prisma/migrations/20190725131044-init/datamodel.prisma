datasource management {
  provider = env("PMT_PROVIDER")
  url      = env("PMT_URL")
}

generator photon {
  provider = "photonjs"
  output   = env("PMT_OUTPUT")
}

model Tenant {
  id       String @default(cuid()) @id @unique
  name     String @unique
  provider String
  url      String
}